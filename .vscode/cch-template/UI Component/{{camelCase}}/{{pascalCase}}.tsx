import styles from './{{pascalCase}}.module.scss'
import { I{{pascalCase}} } from './{{pascalCase}}.types'

const {{pascalCase}}: React.FC<I{{pascalCase}}> = ({}) => {
  return (
    <div className={styles.container}>
      
    </div>
  )
}

export default {{pascalCase}}
