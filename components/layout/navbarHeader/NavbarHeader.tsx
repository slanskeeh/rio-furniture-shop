import styles from './NavbarHeader.module.scss'
import { INavbarHeader } from './NavbarHeader.types'

const NavbarHeader: React.FC<INavbarHeader> = ({}) => {
  return (
    <div className={styles.navbarHeader}>
      
    </div>
  )
}

export default NavbarHeader
