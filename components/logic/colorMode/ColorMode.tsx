import styles from './ColorMode.module.scss'
import { IColorMode } from './ColorMode.types'

const ColorMode: React.FC<IColorMode> = ({}) => {
  return (
    <div className={styles.colorMode}>
      
    </div>
  )
}

export default ColorMode
